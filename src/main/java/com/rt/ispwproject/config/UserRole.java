package com.rt.ispwproject.config;

// Available roles for users of the system
// The enum names must match the ones used internally by the database
public enum UserRole { SIMPLE_USER, TRAVEL_AGENCY }
